# Trim the build context. Never exclude bpf/vmlinux.h, bpf source, cmd/, or go.mod.
.git
.github
build/
dist/
coverage.out
demo/
test/
deploy/
scripts/
# Default config baked into the runtime image (see Dockerfile).
examples/*
!examples/config.yaml
